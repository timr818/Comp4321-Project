Group members: Alfonso Miguel Santos-Tankia & Timothy Regan

README File
One must build/compile the code using the designated c.txt script; just run the script by typing this on the command line:	bash c.txt

One must run the code using the designated r.txt script; just run the script by typing this on the command line:	bash r.txt

By running the program, it will recreate/create the desired spider_result.txt file. The file will include the desired 30 pages and its desired outputs.


TL;DR
to compile:	bash c.txt
to run:		bash r.txt
output file:	spider_result.txt
